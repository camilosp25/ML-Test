import {createContext} from 'react';

/**
 * Contexto para facilitar el uso de los datos que retorna el endpoint search en otros componentes
 * @type {React.Context<null>}
 */
export const ListContext = createContext(null);
